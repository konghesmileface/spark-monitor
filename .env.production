# Production overrides — these take precedence over .env.local during `vite build`
# CN Intel API: empty = same-domain relative URLs (e.g. /api/auth/login → sparkmonitor.cn/api/auth/login)
VITE_CN_INTEL_BASE=
